# Testing redshift makes use of the CI environment so this overrides some default properties.
#
# TODO - use placeholders for secrets, even though they aren't really configurable
# TODO   (the values *must* be set to the proper CI credentials)

archive:
  root: s3://rdw-ci-archive
  cloud:
    aws:
      # rdw-ci
      credentials:
        accessKey: AKIAIZOENPVUIWCRJ5JA
        secretKey: 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
spring:
  migrate_datasource:
    url-server: rdw-aurora-ci.cugsexobhx8t.us-west-2.rds.amazonaws.com:3306
    url-schema: migrate_olap_test
    username: sbac
    password: cabscabs4U
  olap_datasource:
    url-server: rdw-dev.cibkulpjrgtr.us-west-2.redshift.amazonaws.com:5439
    url-schema: reporting_olap_rst
    username: awsrst
    password: Password4U
  warehouse_datasource:
    url-server: rdw-aurora-ci.cugsexobhx8t.us-west-2.rds.amazonaws.com:3306
    url-schema: warehouse_test
    username: sbac
    password: cabscabs4U